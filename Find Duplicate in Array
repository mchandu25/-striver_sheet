#include <bits/stdc++.h> 
int findDuplicate(vector<int> &arr, int n)
{
   unordered_map<int,int>box;
    for(int i=0;i<n;i++)
    {
        box[arr[i]]++;
        if(box[arr[i]]==2)return arr[i];
    }
}

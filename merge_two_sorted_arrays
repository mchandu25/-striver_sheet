int i=m-1;
int j=n-1;
int t=m+n-1;
while(j>=0)
{
    if(i>=0 && arr1[i]>arr2[j])
    {
        arr1[t]=arr1[i];
        i--;
    }
    else
    {
        arr1[t]=arr2[j];
        j--;
    }
    t--;
}
